name: Live DevOps Pipeline

on:
  push:
    branches:
      - main

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Step 1 - Fetching Code from GitHub Repository
        uses: actions/checkout@v4

      - name: Step 2 - Simulating Code Quality Test
        run: |
          echo "Checking if index.html exists..."
          test -f index.html
          echo "Success: HTML file verified!"

      - name: Step 3 - Simulating Production Build
        run: |
          echo "Optimizing code layouts..."
          echo "Pipeline completed successfully!"
          